# 该配置文件统一编辑器风格
root = true

[*]
indent_style = space
indent_size = 2
charset = utf-8
trim_trailing_whitespace = true     # 去除多余的空格
insert_final_newline = true        # 尾部插入一行
end_of_line = lf                    # 换行符

[*.md]
trim_trailing_whitespace = false
